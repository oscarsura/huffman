public class BinaryWriter {
    //writeBuffer -> arraylist of strings, or string array
    //templatized? variadic argument list?
    //takes an arbitrary array of bits... then writes each one to the specified file?
    //constructor will take a file to open... then one can write and close the buffer
    //will the constructor take a file or a path?
}
